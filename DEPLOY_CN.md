# 国内落地执行手册（松悟轩·see 预售登记页）

> 目标：让国内访客（律师 / 法务）能稳定打开页面，并能在微信里正常传播。
> 结论先行：**代码和部署都是健康的，问题是 `.vercel.app` 这个后缀在国内被 DNS 污染了。换上你自己的域名就通。**

---

## 0. 现状：为什么现在打不开

| 检查项 | 结果 |
|---|---|
| GitHub 代码 | 正常，与远程同步 |
| Vercel 部署状态 | `success`（Deployment has completed） |
| 国外访问 | 正常，页面完整可打开 |
| **国内访问** | **打不开** |

原因不是站点故障，是 `*.vercel.app` 被 DNS 投毒——实测同一个域名在 5 个 DNS 服务器返回 5 个**互不相关**的 IP，其中甚至有 Dropbox 的地址段。正常域名在任何 DNS 都该解析到同一组 IP。

**所以：这个地址在国内基本没救，也不是修代码能解决的。换个自己的域名。**

---

## 第一步：买域名（今天，约 30 分钟）

### 去哪买
**腾讯云 DNSPod**（https://dnspod.cloud.tencent.com/ ）
——微信扫码就能登录，后续备案也在同一个账号里，不用跨平台倒腾。

### 名字怎么选
要印在名片、物料上给律师扫，**短、好念、不容易拼错**是唯一标准。

按这个顺序试，哪个没被注册就用哪个：

1. `songwuxuan.com` —— 首选，品牌名直给
2. `swxsee.com` —— 短，6 个字母，好印
3. `songwuxuan-see.com` —— 带连字符，读起来没上面顺
4. `songwuxuan.cn` —— 便宜一半，但要实名，且备案流程稍麻烦

> 别选 `.xyz` `.top` 这类便宜后缀。律师行业，域名本身是信任的一部分。

### 花多少钱
`.com` 首年约 70 元（腾讯云常有活动价，续费约 80–90 元/年）。

### 一个提醒
**只买域名，不要顺手买"域名套餐""建站套餐""企业邮箱"**——用不上，还会在续费时变成负担。

---

## 第二步：绑到 Vercel（今天，15 分钟 + 等待生效）

### 2.1 先在 Vercel 添加域名
1. 打开 https://vercel.com/nicholas0701s-projects/songwuxuan-see-waitlist/settings/domains
2. 输入框填你买的域名（如 `songwuxuan.com`），点 **Add**
3. 添加后页面会出现一张**域名卡片**，上面有**这个项目专属的 DNS 解析值**

> ⚠️ **关键：以卡片上显示的值为准，不要用网上搜到的通用值。**
> Vercel 给不同项目分配的 IP 可能不同（常见的有 `76.76.21.21`，新项目也可能是 `216.198.79.1` 等其他地址）。填错了会一直显示 `Invalid Configuration`。

### 2.2 再去腾讯云加解析记录
登录腾讯云 → **DNS 解析 DNSPod** → 找到你的域名 → 添加记录：

| 记录类型 | 主机记录 | 记录值 | 说明 |
|---|---|---|---|
| **A** | `@` | 域名卡片上的 IP | 根域名，如 `songwuxuan.com` |
| **CNAME** | `www` | 域名卡片上的 CNAME 目标 | 如 `www.songwuxuan.com` |

TTL 选 `600`（生效快）。

### 2.3 三个容易踩的坑
- **删掉旧的 A 记录**：如果域名之前指向过别的主机，旧记录会导致访问随机分流。
- **不要加 AAAA（IPv6）记录**：Vercel 对第三方 DNS 不支持 IPv6，加了会导致 SSL 证书签发卡住。
- **先别开 CDN / 代理**：如果用 Cloudflare 之类，先设成"仅 DNS"（灰色云），橙色云会干扰证书签发。

### 2.4 验证
回到 Vercel 域名页，显示 **Valid Configuration** 就成了。DNS 生效通常 15–30 分钟，最长 48 小时。

生效后：
- Vercel **自动配 HTTPS 证书**，免费、自动续期，你不用管
- 前端请求是相对路径 `/api/signup`，**代码零改动**就能用新域名

---

## 第三步：提交备案（同步启动，别等）

### 为什么要备案
不备案的域名在微信里打开，轻则提示"非官方网页"，重则直接被拦截。

**你的传播主阵地是微信**（朋友圈、群、扫码），所以备案是绕不开的，不是可选项。

### 时间
1–20 个工作日，多数情况 7–10 天。

### 材料清单（提前准备，省得来回补）
- 身份证正反面照片
- 手机号（需实名，且能收验证码）
- 域名证书（腾讯云域名管理页可下载）
- 网站名称（建议填"松悟轩"或"松悟轩法律科技"，**不要**含"中国""律师协会"等敏感词，会被驳回）
- 备案承诺书 + 人脸核验（在 App 里完成）

### 流程
腾讯云控制台 → 搜索"**网站备案**" → 按引导填。全程手机 App 也能办，会有人打电话核验，如实说"企业官网展示 + 用户预约登记"即可。

> 备案期间页面照常用（绑在 Vercel 上），**不耽误收登记**。

---

## 第四步：备案下来后，迁到腾讯云 CloudBase（1–2 小时）

代码已经备好了，不用重写。

### 4.1 为什么迁
- 服务器在国内，访问速度快且稳定，不受跨境链路波动影响
- 微信里传播无障碍
- 和域名、备案在同一个账号，管起来省事

### 4.2 已经准备好的东西
仓库里 `cloudfunctions/` 目录下有两个云函数，**逻辑跟线上 Vercel 版完全一致**（共用同一份核心代码）：

```
cloudfunctions/
├── signup/index.js    登记接收
└── export/index.js    名单导出（需 ADMIN_TOKEN）
```

> 原理：腾讯云按函数目录打包，不支持跨目录引用，所以 `tools/sync-cloudbase.js` 会把 `api/_core.js` 同步成各目录下的 `core.js`。
> **改逻辑请一律改 `api/_core.js`**，然后跑 `node tools/sync-cloudbase.js`，别手改生成物——否则会出现"Vercel 上正常、腾讯云上丢数据"这种极难排查的问题。

### 4.3 迁移步骤
1. 腾讯云控制台 → **云开发 CloudBase** → 新建环境（选**按量计费**或**个人版**，别选免费版——免费版不能用静态托管）
2. **静态网站托管** → 上传 `index.html`
3. **云函数** → 新建函数 `signup`，运行环境 Node.js，**上传方式选"本地上传文件夹"**，选 `cloudfunctions/signup` 整个目录
4. 同样方式建 `export` 函数
5. **HTTP 访问服务** → 配置路由：
   - `/api/signup` → `signup` 函数
   - `/api/export` → `export` 函数

   > ⚠️ 路径**必须**配成 `/api/signup`，这样前端一行都不用改。
6. **环境变量**：在云函数配置里加 `FEISHU_WEBHOOK`（值跟 Vercel 里一样）；想用导出功能再加 `ADMIN_TOKEN`
7. 域名 → 在 CloudBase 静态托管里绑定你的域名，按提示改解析

### 4.4 迁完验一遍
打开你的域名，填一次表单：
- 页面显示"登记成功，早鸟权益已为您预留！"
- **手机飞书立刻收到通知**

两条都中 = 全链路通了。

---

## 第五步：万一第二步绑完还是打不开

绑了自己的域名后 DNS 污染问题就消失了，但 Vercel 的服务器在海外，**国内访问速度看运气**，个别地区/运营商可能仍不稳定。

如果遇到这种情况，直接跳到第四步，用 CloudBase 的过渡方案：

- CloudBase 会分配一个 `*.tcloudbaseapp.com` 的**免费默认域名**
- 这个域名腾讯云已备案，**国内可直接访问**，微信里也能打开
- 官方标注"仅开发测试用、有频率限制"，但预售验证期完全够用

也就是说：**域名还没备案完，也能先有国内能打开的地址。**

---

## 费用清单

| 项目 | 费用 | 说明 |
|---|---|---|
| 域名 `.com` | 约 70–90 元/年 | 必需 |
| Vercel | 免费 | Hobby 套餐够用 |
| 腾讯云 CloudBase 个人版 | 约 100–240 元/年 | 备案后迁过去才需要 |
| **合计** | **首年约 170–310 元** | |

---

## 一页速查

```
今天：  买域名 → Vercel 加域名 → DNSPod 加 A + CNAME → 等生效 → 用新地址打开
同期：  提交备案（7–10 天，期间页面照常用）
备案后：CloudBase 建环境 → 传 index.html → 建 signup/export 函数 → 配 /api/* 路由 → 填环境变量 → 绑域名
```

**需要帮助时**：把报错信息或截图发我，我来定位。
